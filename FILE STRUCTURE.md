- Monocurl AI Context.md: compact authoring guide for AI agents generating Monocurl scenes, structured to match the default tutorial chapters (overview, language basics, meshes/operators, animations) plus debug transcript guidance, patterns, anti-patterns, and references to the stdlib/tutorial scenes
- bytecode: contains the enum of the Monocurl VM bytecode
  - src/lib.rs: Instruction / prototype definitions plus SectionBytecode metadata (flags, instruction annotations, source file name, imported-bundle display index). `LambdaPrototype` carries per-argument names alongside default/reference metadata so runtime live functions can address default arguments by name. `PushCopy` now carries an explicit copy mode (`Read` / `Reference` / `Raw`) so the compiler can preserve raw lvalue/reference values for selected slots without ad-hoc runtime elision rules, plain reads now materialize stateful mesh leaders without a separate current-value opcode, `IncrementByOne` now gives compiler-generated `for` induction vars a single in-place increment opcode instead of spelling that update as generic lvalue arithmetic, and `Observe` records the top-of-stack value into the execution transcript for `print` statements
- cli: binary crate for the `monocurl` command-line interface
  - src/main.rs: hand-rolled CLI parser and help text for `help` / `image` / `video`, `--system-latex` backend selection for exports, output-path normalization, small export-resolution presets, terminal progress rendering layered over the shared exporter crate, and final transcript printing for CLI runs that executed `print` statements
- assets: checked-in resources copied into packaged apps
  - tectonic/README.md: upstream Tectonic MIT license notice for the bundled Tectonic-derived assets
  - tectonic/bundle.zip: generated seed bundle built from the warmed Tectonic cache for `default_bundle_v33`, covering the common LaTeX files Monocurl's text/Tex/Latex wrappers use without needing a first-run download
  - tectonic/default_bundle_v33.index: full upstream indexed-tar file list for Tectonic format serial 33, used to decide whether a local seed-bundle miss is a real support file that should trigger network fallback
- compiler: converts AST into bytecode, performs static analysis
  - src/compiler/mod.rs: compiler module root; shared symbol/compile-result types, the `compile(...)` entrypoint, compiler-frame state, and bundle/section emission helpers. Root scene bundles now get an implicit empty init section when the source starts directly with slides, giving the executor a concrete `0:inf` pre-scene boundary even without an authored init. Symbols also retain whether they were declared in a stdlib section so later lowering can distinguish the real stdlib binding from a shadowing user local, whether immutable reads should preserve raw lvalue/reference values instead of eliding them, and special-function tags for wrappers such as deterministic `random` / `randint` so root-only call rules can survive aliases and captures during compile-time validation
  - src/compiler/cursor.rs: cursor-visible symbol enumeration for autocomplete plus root-reference tracking metadata used by signature hints
  - src/compiler/statements.rs: statement and control-flow lowering (`let`/`var`/`mesh`/`param`, loops, `if`, `return`, `break`, `continue`, `play`, `print`), including declaration-mode checks such as allowing `mesh` declarations in user libraries while still rejecting library-local `param`. `for (i in range(a, b))` now recognizes the real imported stdlib `range` and lowers it straight into a counted `while`-style loop instead of materializing a range list, both counted and generic `for` loops now use the dedicated `IncrementByOne` opcode for their synthetic induction variables, and `print` lowers to expression evaluation followed by `Observe`
  - src/compiler/expressions.rs: expression lowering, identifier/property/subscript access, literals, unary/binary ops, and lambda/operator/native invocation bytecode emission. Plain `param` reads now compile like ordinary current-value reads, while `$param` remains the syntax that marks a reactive expression. Lambda-call lowering now rejects stdlib `random` / `randint` wrappers outside the root frame, including inside deferred default expressions, at compile time
  - src/compiler/closures.rs: lambda/operator/block/anim lowering, closure-frame setup/teardown, capture registration, and implicit block `_` accumulation. Lambda prototypes now retain argument names in bytecode, including default-argument names used by live default mutation utilities. Non-reference lambda args/defaults and `let` captures now stay as raw values instead of being boxed into lvalues, so nested closures and anim blocks preserve passed references/functions without extra runtime fixups. `block { ... }` bodies still default to `[]`, while ordinary block-bodied lambdas now fall through to an explicit runtime error anchored to the full lambda span
  - src/compiler/free_vars.rs: free-variable and free-lvalue walkers used for lambda/block capture analysis
  - src/compiler/stateful.rs: compiler-side stateful-expression lowering and validation helpers
  - src/compiler/tests.rs: compiler unit tests split out of the module root
  - src/compiler/warnings.rs: AST walker for compile-time warnings about useless expression statements with no syntactic side effects; `print` is treated as an effectful statement
- executor: takes bytecode and scene state and executes it
  - src/error.rs: ExecutorError enum plus RuntimeError / RuntimeCallFrame (raw execution error, selected span, optional hint, bounded recovered callstack)
  - src/heap.rs: thread-local virtual heap for heap-backed values/lvalues; owns `VRc`/`VWeak`, slot refcounts/free-list reuse, raw snapshot wrappers for cacheing, refcount inhibition used while cloning/restoring cached executor state, and test-only live/free slot accounting for leak checks
  - src/camera.rs: shared camera surface helpers; owns `CameraSnapshot`, the compiler-prelude initial camera snapshot helper, normalized right-handed `CameraBasis` (public `+z` is forward while the renderer views along negative camera-space z), default camera constants (including the Monocurl-1.0-equivalent default framing FOV), and the executor-side `Value`/stack-argument parser for camera objects using the public `look_at` camera surface
  - src/executor/mod.rs: Executor struct and public executor API entrypoints, plus seek/result enums, user/global-to-internal timestamp conversion that maps user boundary slide `0` onto the root init section by offsetting only library sections, text-render quality hints (`Normal` / `High`) that native text constructors can read, and NativeFunc/NativeFuture types
  - src/executor/dispatch.rs: main instruction dispatch loop for bytecode op execution, including stack reads/writes, invocation dispatch, control flow, and current-value read materialization. `PushCopy` now dispatches by explicit copy mode so raw captured/argument values can be cloned without lvalue or leader elision while ordinary reads evaluate stateful mesh leaders to concrete values, `IncrementByOne` mutates compiler-generated loop counters in place without routing through generic `Add` + `Assign`, and `Observe` materializes a value into the transcript using the instruction annotation span
  - src/executor/memory.rs: periodic executor memory guard for instruction stepping; now checks only virtual-heap slot count against a hard limit on a throttled cadence
  - src/executor/runtime_error.rs: recovered runtime error span/callstack construction from stack ancestry and saved call frames, including play-aware recovery and outermost-first displayed callstacks so spawning `play` sites appear before nested animation frames and inner imported helper frames; plus public helpers to record an `ExecutorError` either at the current execution context, explicitly at the root stack, or explicitly at the root stack with a diagnostic hint. Those record helpers now return the `RuntimeError` they stored, so callers can react directly without re-inspecting executor state. Span recovery now skips empty sentinel/import annotations, falls back to the most recent real root-module span across earlier sections when the current section is empty, and only then uses section-wide annotation extents, so post-execution/root-recorded errors do not collapse to `0..1` just because execution is parked at an empty slide tail
  - src/executor/cacheing.rs: per-section execution cache used for rebasing seeks plus user-visible slide duration metadata, including exact cached slide durations stored separately from the executor's end-of-slide `time = inf` sentinel and invalidation-aware minimum duration lower bounds
  - src/executor/ops.rs: binary/unary operations with int→float→complex type promotion plus recursive list linear ops (scalar multiply, elementwise add/subtract, negate); owns BinOp enum (including stateful `and`/`or` lifting). Division now consistently raises runtime `division by zero` across integer, float, and complex arithmetic instead of producing infinities/NaNs. Intercepts Value::Stateful operands to build StatefulNode::BinaryOp/UnaryNeg/Not lifted nodes instead of evaluating eagerly
  - src/executor/invoke.rs: async lambda/operator/native invocation, call frame setup, labeled invocations, isolated eager lambda execution with trace-parent stack links, and operator invocation preservation. It now also owns `eagerly_invoke_lambda_many(...)`, which reuses one eager call frame across many argument bundles by retaining the arg/default/capture prefix between returns while materializing each batch result before reseeding so sampled stdlib callbacks can stay fast without aliasing reused temp args. Non-reference lambda args/defaults/captures now stay raw all the way through eager and stack-based invocation; only true reference parameters are boxed on entry. Lambda calls against functions with default arguments now produce `InvokedFunction` metadata even when no explicit labels were written, keeping null labels while preserving default-argument slots for later mutation. Operator calls now always materialize `InvokedOperator` metadata (even with unlabeled args) so operand attributes remain reachable; live/stateful cache writes now recursively materialize nested wrappers before storing them, so later scene capture and mesh reads see concrete values instead of stale `live function` / `live operator` placeholders; `exec_convert_to_live_operator` is retained as a compatibility no-op for that stack shape
  - src/executor/lerp.rs: general lerp(a, b, t) for Monocurl values — handles numbers, same-lambda InvokedFunction arg interpolation, InvokedOperator unmodified embeds, same-topology mesh interpolation for animation followers, and list-length mismatch diagnostics phrased in list terms
  - src/executor/anim.rs: section advancement, seek_primitive_anim (async, yields between instructions), playback stepping/seek_to, play/spawn/bake, primitive target flattening/deduction with ancestor-based implicit leader selection, leader lock handling, and double-play guard via AnimBlock.already_played. Finished sections now park the executor at `Timestamp { slide, time: inf }`; `advance_section` is only used at the beginning of a later forward seek/play step to enter the next slide at time `0.0`, so there are no negative pre-slide timestamps. `play` now elides live function/operator wrappers before dispatch, so animation operators such as `slow{...}` / `fast{...}` / `delay{...}` can feed primitive anims or anim blocks directly. Primitive `Lerp` bake now resolves the final target set, snapshots each original destination, optionally runs an embed callable `(start, dst) -> [a, b, state]`, stores the baked `[a, b, state]` triple, and at step time either uses the provided custom lerp callable `(a, b, state, t)` or the generic executor lerp; completion always snaps followers to the original baked destination rather than reusing the custom lerp result. Internal animation callback invocation now materializes stateful/live arguments before eager lambda/operator calls, so follower helpers such as `Fade` / `Grow` / custom animation lerps can inspect reactive mesh destinations without tripping the direct-stateful-copy guard
  - src/executor/access.rs: subscript/attribute (mutable + immutable), assign, append, heap-backed COW for shared list/map containers and element slots, WeakLvalue handling, leader passthrough for nested subscript/attribute mutation. Immutable subscript reads now accept cached live-wrapper results as concrete list/map/string bases so defaulted stdlib constructors still work where a value is expected. Mutable labeled live-function/live-operator attributes now reuse a single synthetic boxed slot instead of nesting lvalues on repeated edits, and live reevaluation peels exactly one such synthetic layer so by-value params still see values while explicitly passed references remain intact. Mesh-leader reassignment now seeds an empty follower from the prior leader value before overwriting, so first-play animations like `Trans` can still start from the pre-assignment mesh. Value::Stateful attribute access destructures LabeledCall/LabeledOperatorCall nodes; stateful assignment to non-mesh-leader is a runtime error
  - src/state.rs: ExecutionState (execution stacks, ghost stack lineage metadata, leaders, explicit active params, primitive anims, ephemeral_pool, structured runtime errors, transcript, last runtime-error stack context, plus deterministic PRNG state that advances from both prior random calls and the current timestamp), ExecutionStack (var stack, IP, call stack, labels, control parent + trace parent, plus an optional retained-prefix length used by reusable eager-call frames), BakedPrimitiveAnim (target leaders + baked original destinations + embedded start/end/state triples for primitive interpolation), LeaderEntry/ActiveParam metadata (declared name plus leader/follower refs). sync_all_leaders now clones leader values into follower context by converting embedded Stateful values to follower-read mode and bumping follower versions. Monotonic stack IDs and primitive animation IDs; finished stacks remain as ghosts so ancestry checks still work after free.
  - src/transcript.rs: executor-side transcript storage for `print` output, with Arc-counted per-section transcripts, transcript entries carrying source spans/root-section metadata, and stringification for runtime values
  - src/scene_snapshot.rs: shared background/camera/visible-mesh capture for any frontend. Validates scene leaders by eliding live/stateful wrappers, reuses `src/camera.rs` for camera parsing while still parsing background values locally, collects visible mesh trees without re-checking topology on animated frame-state meshes, records scene-shape errors at the root stack's recovered last-executed span with a hint that the actual source may be elsewhere, re-exports `CameraSnapshot` for downstream callers, and returns a `Result` from stable snapshot capture so callers can see directly whether scene capture recorded a root-position runtime error
  - src/value/mod.rs: Value enum plus heap-backed `Lvalue(VRc)` / `WeakLvalue(VWeak)` handles, `InstructionPointer`, and value submodule wiring
  - src/value/helpers.rs: Value helper methods for truthiness, lvalue/wrapper elision (including stateful reads), cached live-wrapper elision for synchronous concrete reads, lvalue access, and runtime type naming
  - src/value/equality.rs: structural equality for values, primitive anim candidates, and reactive stateful trees
  - src/value/container.rs: List (SmallVec of heap keys) and Map (hashable keys to heap keys plus insertion_order for deterministic iteration) with retain/release-aware clone/drop behavior
  - src/value/lambda.rs: Lambda (captures, defaults, IP, per-arg reference flags, per-arg names, default-name lookup helpers) and Operator wrapper
  - src/value/anim_block.rs: AnimBlock (captures + IP for coroutine-style animation blocks, already_played: Rc<Cell<bool>> to prevent double-play)
  - src/value/leader.rs: Leader struct (kind: LeaderKind, leader/follower heap keys, detached status via last_modified_stack, active animation lock, leader_version + follower_version u64 counters for stateful cache invalidation) for mesh/param variables
  - src/value/invoked_function.rs: InvokedFunction cache/recompute wrapper for labeled function calls; cache hits and misses now both store fully materialized values so nested live wrappers do not leak back out through later reads. The body also tracks which labeled arguments were boxed into synthetic mutable slots so reevaluation can peel exactly one layer without losing deliberately passed references
  - src/value/invoked_operator.rs: InvokedOperator (operator + operand + labels + unmodified identity embed + cached modified value); extract_operator_result and build_invoked_operator helpers. Both cached branches now store fully materialized initial/modified values for the same reason as InvokedFunction, and extract_operator_result now also accepts an already-live operator result by reusing that cached pair directly. The body likewise tracks synthetic boxed operand/argument slots so mutable live-operator attributes can round-trip without feeding extra lvalue layers back into ordinary by-value params
  - src/value/primitive_anim.rs: PrimitiveAnim enum (Lerp, Set, Wait) with explicit candidate-tree support for targeted sync animations; `Lerp` carries candidate hints, duration, optional rate callable, optional embed callable, and optional custom lerp callable so primitive animations can bake mapped interpolation state before playback starts
  - src/value/stateful.rs: Stateful (roots: Vec<HeapKey> + root: StatefulNode + read_kind leader/follower mode + version-based cache) and StatefulNode enum for reactive dependency graphs; variants: LeaderRef, Constant, LabeledCall, LabeledOperatorCall, Subscript; constructor dedups root heap cells by key; labeled-call/operator args are stored as heap keys to support mutable attribute access; async Executor::eval_stateful evaluates the full node tree against either leader or follower versions depending on context
  - src/time.rs: Timestamp (slide + time offset); user/global timestamps reserve `slide = 0, time = inf` as the pre-scene boundary, while real visible slides are addressed as `1..=slide_count`
- exporter: isolated scene export pipeline used by the desktop app for still-image and MP4 video export, separate from the live UI services
  - src/lib.rs: parses/compiles live document text plus open imported tabs, renders stills or full frame sequences through the shared renderer/executor stack, requests high-quality text meshing from the executor for export, writes PNGs with BGRA→RGBA conversion, and encodes H.264 via `openh264` before muxing `.mp4` output with the `mp4` crate while emitting coarse export progress updates. Video export now keeps the requested even render dimensions instead of padding them to mod-16 sizes, uses a much higher-quality screen-content encoder tuning, forces fresh keyframes at slide boundaries so slide changes do not smear across inter-frame prediction, and returns collected transcript output in the export outcome
- latex: dedicated LaTeX typesetting and mesh extraction crate
  - src/lib.rs: public facade that re-exports the LaTeX crate API while keeping implementation details split across `types`, `config`, `render`, `cache`, `number`, `document`, `tectonic`, `system`, and `svg`
  - src/types.rs: public backend/render result types (`LatexBackendConfig`, system backend probes, `RenderQuality`, `RenderedOutput`) plus the crate-internal text/Tex/Latex render kind used by the cache
  - src/config.rs: global backend configuration (`Bundled` Tectonic executable vs explicit system `latex` + `dvisvgm` paths), system backend discovery/status helpers, and in-memory cache clearing when the selected backend changes
  - src/render.rs: public `render_text` / `render_tex` / `render_latex` entrypoints plus marked Tex rendering and tag recovery. `render_tex(...)` still parses `\text_tag{...}{...}` wrappers and preserves tag recovery on both backends by rewriting tagged spans to synthetic marker pins and remapping the rendered marker meshes back to the original tag lists afterward
  - src/cache.rs: backend-aware in-memory mesh cache keyed by backend/source/scale/quality plus persistent SHA-256 SVG cache under Monocurl's temporary `latex_svg` directory using a two-character hash prefix directory layout
  - src/number.rs: number formatting/rendering helpers that cache individual TeX glyphs before laying them out into numeric strings
  - src/document.rs: shared LaTeX document wrappers (`Text`, display-math `Tex`, and raw `Latex`), legacy pin/row-pin macro definitions, `\text_tag{tag}{body}` parsing/stripping into nested span ranges with innermost tags naturally overriding outer ones during recovery, numbered shorthand parsing for `\tag0{...}` / `\tag1{...}` / ..., including empty and arbitrarily long tag lists, and legacy pin-color rewriting for marker recovery
  - src/tectonic.rs: bundled backend using the in-process `tectonic` Rust crate; opens a local bundle directory or zip as a first-choice seed bundle, uses the packaged default-bundle index to distinguish support-file misses from ordinary TeX probes, lazily falls back to Tectonic's default CDN-backed bundle for missing support files, runs LaTeX→PDF in-process via `ProcessingSessionBuilder`, converts the resulting PDF page to SVG with `hayro-svg`, and returns the SVG to the shared importer
  - src/system.rs: explicit system-LaTeX runner and discovery helpers for `latex` + `dvisvgm`; shells out in a temporary directory with caller-supplied binary paths and returns the generated SVG
  - src/svg.rs: shared SVG-to-mesh importer for both backends; samples SVG path curves with normal/high-quality profiles, applies a smaller text-only stroke radius to imported glyph meshes, preserves SVG fill semantics (including `evenodd`) plus fill-time implicit closure of open subpaths before tessellation, rebuilds filled contours into `geo::mesh::Mesh` via `geo::mesh_build::build_indexed_surface` so boundary links come from the tessellated surface rather than source-index stitching, and decodes legacy pin-tag colors for span recovery
- geo: helper routines for execution (a lot of lib monocurl will reference these routines).
  - src/mesh.rs: core mesh structs (`Dot`, `Lin`, `Tri`, `Mesh`, `Uniforms`), lightweight per-mesh render metadata such as alpha/texture/z-order, globally fresh mesh version generations plus the `make_mesh_mut(...)` Arc COW helper used to invalidate renderer GPU buffers only when mesh contents are mutated, slightly heavier default stroke width for non-text meshes, and topology invariant validation helpers; topology is expected to be authored by constructors/ops rather than inferred from geometry, with inverse plus dominant-sibling links retained but no separate antinorm topology. `Mesh::normalize_line_dot_topology()` now seals authored open/standalone line-dot meshes by adding inverse line siblings, invisible endpoint dots, and standalone dot inverse pairs before final validation. Negative refs are treated as typed ownership links rather than generic sentinels: triangle edges must point back through line ownership, line endpoint dot refs must point back through `dot.inv`, and line inverse refs into triangles must be reciprocated by the owning triangle. For lines, inverse siblings must alternate dominance, non-inverse/boundary lines must themselves be dominant, and triangle-owned boundary lines are explicitly required to keep reciprocal `prev` / `next` line links so filled-surface strokes cannot silently degrade into disconnected butt caps
  - src/mesh_build.rs: shared geometry-authoring helpers extracted out of stdlib, including colored line/polyline assembly, boundary-edge/indexed-surface structs, dominant-by-default authored line segments, and topology-preserving indexed-surface reconstruction used by both stdlib mesh ops and the latex crate. `build_indexed_tris(...)` is now explicitly closed-surface-only and rejects any mesh whose triangle boundary would otherwise be silently downgraded from an explicit line ref to “none”, while `build_indexed_tris_with_open_boundaries(...)` exposes the staged triangle-only half of that authoring flow for internal callers like `uprank` that immediately reattach explicit boundary lines before returning a final mesh
- libtess2: safe Rust wrapper over a vendored copy of the upstream libtess2 tessellator
  - src/lib.rs: `Float3`-based contour input, tessellation options/error types, opt-in contour normalization for degenerate/redundant loop sets before triangulation, and safe triangle output extraction from `TESStesselator`
  - build.rs: compiles the vendored upstream C sources from `upstream/Source` and links them into the Rust crate
  - upstream/: git-cloned `https://github.com/memononen/libtess2` source tree with `.git` removed and `LICENSE.txt` retained
- lexer: lexing utilites, contains token definition
  - src/lexer.rs: actually lexes a stream of characters, including the `print` keyword
  - src/token.rs: contains an enum of all of the tokens and helper functions related to them; `Print` is a control-flow-style keyword token
- parser: transforms lexed source file into an AST
  - src/ast.rs: contains structs related to all of the types of ast nodes, including the `Print` statement node
  - src/parser/mod.rs: parser module root; shared parser state/diagnostic structs, bracket precomputation, token-reading helpers, and top-level `Parser` / `SectionParser` definitions
  - src/parser/predicate.rs: token/state predicates used by the tolerant `try_all!` parser machinery and cursor-possibility emission
  - src/parser/statements.rs: section/statement-list parsing plus statement bodies, declarations, loop/if/play/print/return parsing, and identifier declarations
  - src/parser/expressions.rs: precedence parser, postfix/property/subscript/invocation parsing, and lambda/block/anim expression parsing; leading `*` is no longer parsed as an identifier form. Postfix parsing now preserves newline boundaries instead of consuming them, so a leading `.` on the next line is reliably parsed as a new dot-statement rather than accidental property access
  - src/parser/literals.rs: string/numeric/list/map literal parsing helpers
  - src/parser/imports.rs: import DFS/preparse logic, file splitting into sections, cache-aware bundle assembly, and the public parse entrypoint
  - src/parser/tests.rs: parser unit tests split out of the module root
- renderer: shared blade-only GPU renderer for viewport and export output
  - src/lib.rs: public renderer facade; version-based scene fingerprinting that hashes mesh Arc identity/version instead of walking geometry, render sizing/styling types, raster-scale-aware render views, and blade-only initialization/error handling. The viewport path can now request a full output frame with a smaller projection rect so preview overscan is rendered from one continuous image instead of separate inner/outer passes
  - src/blade/mod.rs: blade backend module root; owns renderer state, cache keys, shared constants, and the submodule wiring for the offscreen blade renderer
  - src/blade/renderer.rs: main blade renderer methods; initializes GPU state, manages uploads/MSAA targets/caches, reuses mesh GPU buffers by live Arc identity plus mesh version, submits triangle-line-dot passes, and reads finished BGRA frames back to the CPU
  - src/blade/geometry.rs: mesh-to-GPU packing helpers for triangles, strokes, and dots, including inferred line-neighbor joins, dominant-sibling-aware line and dot selection so normalized inverse bookkeeping siblings do not render twice, one-sided triangle packing with averaged smooth normals, and focused geometry tests
  - src/blade/pipelines.rs: blade pipeline creation/destruction and WGSL validation coverage for the split backend
  - src/blade/resources.rs: small GPU resource wrappers plus helpers for textures, offscreen targets, readback extents, sample-count selection, weak mesh cache ownership, and cache-owned GPU cleanup
  - src/blade/types.rs: shared shader POD/uniform structs, bind-group layout carriers, camera packing, and vertex attribute packing helpers used across the blade backend
  - src/blade.wgsl: blade shaders for triangle, line, and dot passes, including right-handed camera projection into blade's depth clip space, alpha cutouts, glossy triangle lighting, legacy one-sided line-stroke miter extrusion driven by dominant sibling orientation, and the camera-side viewport scale used by preview overscan rendering
- stdlib: actual lib monocurl routine implementations. depends on executor for Value types
  - src/registry.rs: defines `FunctionEntry` (using executor's `NativeFunc` type), `inventory::collect!`, the `Registry` singleton (lazily built, sorted by name via `OnceLock`), and `func_table()` to build a Vec<NativeFunc> for the executor
  - src/anim/: split animation native module. `primitives.rs` covers the native Wait/Set/general primitive-animation builders; `std/anim.mcl` exposes that general constructor as `PrimitiveAnim(...)`, while `Lerp(...)` is now just the vanilla rate-driven wrapper with no custom embed/lerp slots. `followers/` now splits the low-level mesh embed/lerp natives by animation family: `mod.rs` keeps the small Grow/Fade/Highlight embed helpers plus shared helpers, `camera.rs` covers CameraLerp embed/value interpolation, `write.rs` covers Write staging plus the pure flash-value helper built on the same contour-separated write state while ignoring non-dominant inverse line siblings during authored path ordering, `trans.rs` covers Trans/TagTrans matching plus shared path-arc interpolation, including the final `similar_topo_hint` option for exact-topology-first matching when true, planar-loop matching plus indexed-surface rebuilds for generic surface conforming so holey templates and opposite triangle winding stay topologically consistent during rematching while canonicalizing both sides onto one template ordering before lerp-time topology checks; same-topology line/surface pairs now still route through that alignment logic instead of always lerping raw authored slot order, its line/path samplers likewise ignore normalized inverse bookkeeping siblings, its open-path matcher now preserves authored traversal direction, and its triangle matcher now mirrors the old C runtime by trying only cyclic vertex rotations rather than mirrored winding flips. `bend.rs` is the thin Bend/TagBend wrapper over that path-arc machinery; the planar `Trans` path now uses explicit LCM loop replication and the trans matcher no longer falls back to the old generic topology conform helper. `std/anim.mcl` now keeps internal mesh-lerp callables private, standardizes time-bearing animation constructors around `time` first and `rate` last, uses a linear default rate for `Write` so tracing starts visibly at the beginning of the animation, requires explicit camera candidates for `CameraLerp`, restores `delay{...}` as operator sugar that lowers to an anim block, treats `rate{...}` as the canonical primitive-animation operator while keeping `with_rate{...}` as a compatibility alias, expresses `Highlight` as a reference-taking composition over `Trans`, expresses `Flash` as `Set` plus a plain `Lerp` between two live function invocations of the same flash helper so phase advances through normal function-lerp label interpolation rather than a custom primitive lerp callback, and now defines `TransferSubset` / `CopySubset` in stdlib on top of a shared native tag-splitting helper so subset moves can support both exact tag sets and predicate filters. `indication.rs` is now just a placeholder module because indication animations are expressed in `std/anim.mcl`; `transfer.rs` covers Transfer/Copy; `composition.rs` covers LaggedMap plus primitive animation operators; `rates.rs` covers bounce/elastic easing; `helpers.rs` owns target resolution, follower/leader mutation, and the remaining shared animation value helpers
  - src/color.rs: color-related native helpers such as `hsv`; `assets/std/std/color.mcl` now exposes `alpha{...}` as the canonical color-alpha replacement operator
  - src/math/: split math native module. `helpers.rs` handles typed argument extraction, including mixed int/float pair reads for scalar natives that can preserve integer results; `scalar.rs` covers elementary math/trig and now keeps integer semantics for cases like integer `mod`, `min`, and `max` instead of promoting eagerly; `combinatorics.rs` covers integer helpers plus deterministic executor-state-backed `random` / `randint` that no longer rely on process-global thread-local state; `stats.rs` covers aggregate statistics, `vector.rs` covers dot/cross, and `interpolation.rs` covers general `lerp` plus map-or-list based `keyframe_lerp`
  - src/mesh/: split mesh native module. `helpers.rs` owns mesh-tree traversal, typed mesh/vector readers, primitive mesh builders, callable invocation helpers, transforms, tag-filter support, topology-slot grouping, indexed line/surface extraction, authored triangle-boundary reconstruction, libtess2-backed planar loop tessellation, and now exact/predicate mesh-tree partitioning so callers can split a tree into matching and non-matching leaves without reimplementing tag-filter semantics; predicate tag filters once again receive the full mesh tag list rather than one scalar tag at a time. Its topology-slot grouping now also follows negative `dot.inv` ownership refs back into line endpoints instead of relying only on the line-side `prev` / `next` links, `mesh_from_parts(...)` now seals open/standalone line-dot meshes through `Mesh::normalize_line_dot_topology()` while defaulting authored dot radius to `0.0` so topology dots stay invisible unless explicitly opted in, and indexed line extraction now ignores inverse bookkeeping siblings so authored paths are not doubled. It also now exposes batched callable invocation so mesh samplers can reuse one eager executor frame across many callback samples, and its RGBA readers now unwrap live/stateful/operator wrappers before validating color lists so nested color operators like `fill{alpha{...} BLUE}` materialize correctly. `constructors.rs` covers core 2d/3d primitives plus text/label/layout helpers, routes planar fills and multi-loop polylines through shared builders so topology is authored centrally rather than reconstructed ad hoc, authors the canonical local-space primitives in fixed local planes where appropriate, keeps triangle and measurement outlines on shared closed/open polyline builders, keeps `Dot(...)` as the only primitive with the default visible dot radius, authors `Image(...)` quads with white triangle tint plus upright raster sampling, authors `Vector`/`Arrow` as one tessellated outline with a shared shaft/head silhouette that caps shaft/head proportions against vector length so short arrows stay narrow, accepts scalar-or-pair capsule radii, routes `Text` / `Tex` / `Latex` through the dedicated latex crate while forwarding executor-selected text quality, renders `Number(...)` through the latex crate's cached per-glyph number layout with optional decimal places and explicit positive signs, and implements `Label(...)` / `Measure(...)` side helpers with explicit buffers. `graphs.rs` covers graphing-oriented constructors (`Axis1d`, `Axis2d`, `Axis3d`, `PolarAxis`, `ColorGrid`, `LineGrid`, `Field`, `ParametricFunc`, `ExplicitFunc`, `ExplicitFunc2d`, `ImplicitFunc2d`, and `ExplicitFuncDiff`), including sampled-domain handling, batched callback sampling, sampled-surface topology, `ColorGrid` flat-cell coloring by default with optional smooth vertex-color sampling, single-mesh subdivided `LineGrid` authoring with point limits, explicit-difference region splitting, per-axis style lists with title/tick/label-map/arrow-extrusion data, axis label/grid rendering with axis bars emitted after grids, 3d axis label-up vectors for orienting tick labels and titles, inferred axis basis scale, and graph-specific topology limits. `ops.rs` covers styling, remap, extrusion/revolve, and transform operators, with no-op fast paths for identity shift/scale/rotate/fade constants, with `uprank`/`downrank` now treating closed boundary loops as first-class topology instead of reintroducing fan diagonals by default, with scalar/vector `scale` sharing one native path, with most mesh operators now accepting optional exact-or-predicate tag filters for subset application, with unified `color` / native recolor support for dots-lines-fills together, with `stroke` now taking `stroke_width` as its second argument (default `2.0`) and an optional third-argument filter, and with `op_restroke(...)` handling both stroke recolor and stroke-radius writes so the operator surface stays a simple pair of endpoints, with `subset_map` rewriting only the matching mesh leaves through a user mapping lambda, with `dashed` now splitting matching mesh strokes into arc-length dash/gap segments while preserving fills and dots in a companion base mesh, while normalizing any standalone stroke output back into paired line topology, with `extrude` authoring reciprocal side-wall winding against the source boundary, with `in_space` now remapping line/dot normals along with positions so canonical local-space meshes can be embedded cleanly, with `camera_transfer` remapping meshes between camera bases to preserve their apparent screen placement, and with camera-aware `to_side` / `to_corner` placement helpers reusing the shared executor camera parser/basis while defaulting `nil` camera arguments to the built-in camera instead of requiring an explicit camera value. Point/color/uv remap operators now batch their callback invocations per mesh so repeated vertex sampling no longer rebuilds eager call frames per vertex; `queries.rs` covers bounds/tag/topology/filter plus sampling/distance/raycast helpers, now exposes `tag_split(...)` alongside `tag_filter(...)`, preserves negative dot-to-line ownership refs when merging meshes, and now numbers each `mesh_contour_separate(...)` output leaf with a fresh single tag `[0]`, `[1]`, ... so contour-separated trees can be addressed deterministically
  - assets/std/std/mesh.mcl: user-facing mesh stdlib wrapper surface; shiftable primitive constructors such as `Circle`, `Square`, `Rect`, `RegularPolygon`, `Arc`, `Sphere`, `RectangularPrism`, and `Cylinder` are now authored at the origin and rely on `shift` for placement, the canonical planar constructors (`Circle`, `Annulus`, `Square`, `Rect`, `RegularPolygon`, `Arc`, `Torus`, `Plane`, `Image`, `PolarAxis`) no longer expose user normals and instead rely on `in_space{...}` for reorientation when needed, canonical operator names now prefer `center`, `z_index`, `camera_transfer`, `to_side`, and `to_corner`, text-like constructors now require their content arguments instead of defaulting them to empty strings, `Number(value, decimal_places = nil, include_sign = 0)` exposes cached glyph-based numeric text with C `%g`-style default formatting, `text_tag{tag} value` now exists as operator sugar that preserves per-fragment origin tags across `Text` / `Tex` / `Latex`, `fade{...}` now requires an explicit opacity, `stroke{color, stroke_width, filter}` now treats the second argument as stroke width with a default of `2.0` and the third as an optional filter, `Brace` / `Measure` expose explicit buffer parameters, `Label(target, str, dir, scale, buffer)` maps to the native LaTeX-backed side-label helper, `gloss{amount, filter}` now accepts an explicit gloss strength while still defaulting to the old `0.5`, `to_side` / `to_corner` take `dir`, `buffer = 0.1`, then optional `camera = nil` so camera arguments only appear after placement direction and buffer, `ColorGrid` accepts a post-sample `smooth` flag while preserving callable fourth-argument masks, `LineGrid` wraps the native single-mesh subdivided line-grid constructor, `Axis1d` / `Axis2d` / `Axis3d` use compact per-axis default lists plus separate global axis/grid colors, `Axis3d` exposes `label_up` before the per-axis styles for 3d tick/title orientation, and the interpolatable `axis_style{axis, min, max, axis_title, tick_spacing, major_tick_rate, label_map, arrow_extrusion}` operator updates those defaults via native `set_default`, `dashed{lengths, offset, filter}` now accepts either a scalar or `[dash, gap]` pair and rewrites strokes into visible dash segments without dropping fills, `tag_filter{...}` is now the language-level subset-selection operator while `tag_split(...)` remains a plain function, `contour_separate{}` now exists as a mesh operator wrapper over contour flattening, and range-bundle wrappers such as `ExplicitFuncDiff` still normalize user-friendly argument lists before calling the native layer
  - src/scene.rs: scene/camera helpers backing `std.scene`; provides the default right-handed prelude camera at positive z plus the default white background scene state, with the public camera constructor surface now centered on required `position` + `target` plus optional `up` / clip range
  - src/util/: split util native module. `helpers.rs` handles list construction, depth/comparison helpers, key-lambda invocation, the block-lambda fallthrough runtime error native, shared async string-argument coercion via the crate-level stringifier so live wrappers can appear inside text fragments, and `expect_list(...)`, which now unwraps live/stateful/operator wrappers before validating fixed-length list arguments; `introspect.rs` covers `type_of`, type predicates, dynamic live function/operator attribute helpers (`has_attr` / `get_attr` / `set_attr`), default-argument introspection/mutation (`get_defaults`, `set_default`, `set_defaults`, including level-based blending from the original default value to the replacement and a level-0 `set_default` fast path before replacement-argument reads), and explicit user-thrown runtime errors; `lists.rs` covers range/sample/sort/zip/slicing/extrema helpers, including inclusive and closed-open uniform numeric sampling; `maps.rs` covers map iteration helpers; `strings.rs` covers string operations; `convert.rs` covers numeric coercions, complex `real(...)` / `imag(...)` accessors, plus `to_string(...)`, which now joins nested list elements recursively while still rejecting complex values, and `text_tag_encode(...)`, which lowers stdlib `text_tag{...} value` fragments into `\text_tag{...}{...}` wrappers for the latex crate backends while allowing empty or longer list tags instead of forcing at least one component
  - assets/std/std/util.mcl: user-facing util wrapper surface, including collection/string/conversion helpers, inclusive `sample(...)`, closed-open `sample_clopen(...)`, `const_list(item, n)` repeated-list construction, complex component accessors, type predicates, live-attribute helpers, and default-argument helpers. `set_default{name, value}` and `set_defaults{map}` are exposed as interpolatable operators over live function/operator chains, while `get_defaults(value)` returns the available default argument names
- stdlib-macros: proc-macro crate providing the `#[stdlib_func]` attribute; generates a `NativeFunc`-compatible wrapper and submits it to the `inventory` collector used by `stdlib::registry`
- structs: helper structs and utilities.
  - src/assets.rs: contains code for location where assets (say fonts and default scenes) are located; also exposes relative `img/...` resource paths for GPUI SVG asset loading
  - src/iterutil.rs: contains code for KLookahead iterator from a base iterator
  - src/rope.rs: a general purpose rope mostly used by the UI for an efficient text editor
  - src/text.rs: small helper structs for abstracting locations in utf8 or utf16
- ui: contains all ui related code (everything is gpui)
  - src/components/buttons.rs: contains a button styled like a link
  - src/components/latex_warning.rs: shared warning banner for invalid system LaTeX tool paths, reused by home and document views. The bundled Tectonic backend stays silent because it can use the packaged seed bundle or download support files on demand
  - src/components/split_pane.rs: gpui element that implements vertical and horizontal split pane
  - src/editor: code related to the text editor. 
    - editor_view.rs: wrapper around `TextEditor`; bootstraps editor content from the internal file, handles periodic internal autosave, writes user saves, and suppresses the pending internal flush when a tab explicitly discards unsaved changes (including pathless scratch tabs that are about to be forgotten)
    - text_editor/mod.rs: text editor module root; owns keybindings, shared editor state fields, construction, theme application, and module wiring
    - text_editor/history.rs: undo/redo grouping, external-reload replacement history, and undo-boundary management
    - text_editor/editing.rs: text replacement, bracket/quote pairing, deletion/newline/tab/comment editing actions, and cursor adjustment for line transforms
    - text_editor/cursor.rs: cursor reads/writes, cursor blinking, keyboard navigation, selection movement, autocomplete row movement, and home/end/select-all handlers
    - text_editor/scroll.rs: scroll anchoring, cursor reveal, autoscroll during mouse selection, wrap-width calculation, and mouse-position-to-text-location conversion
    - text_editor/layout.rs: line reshaping and visible-line relayout, including inline transcript rows below source lines with hidden-count compaction
    - text_editor/mouse.rs: mouse selection, multi-click word/line selection, hover reset triggers, and autocomplete dismissal on click
    - text_editor/hover.rs: hover debounce/timer state, diagnostic hover discovery, and hover/copy-message reset handling
    - text_editor/clipboard.rs: paste indentation normalization, copy, cut, and character-palette action handling
    - text_editor/input_handler.rs: GPUI EntityInputHandler and Focusable implementations for platform text input / IME integration
    - text_editor/render.rs: GPUI Render implementation for the editor shell, scroll pane, text layer, and popover layer
    - text_editor/popover_element.rs: GPUI popover element root; owns popover state discovery, shared placement/overlap logic, and element lifecycle wiring for diagnostics, autocomplete, and parameter hints
    - text_editor/popover_element/diagnostic.rs: diagnostic hover popover rendering, including message copy-to-clipboard state
    - text_editor/popover_element/autocomplete.rs: autocomplete popover row rendering, highlighted match spans, selection styling, scroll handling, and mouse application
    - text_editor/popover_element/parameter_hint.rs: parameter-hint signature rendering and active/default/reference argument labels
    - text_editor/text_element: this contains the rendering logic for the actual text editor (including scroll bar and everything on the base layer). Selection rendering accounts for inline transcript rows as extra height without treating them as selectable source text.
    - wrapped_line.rs: The text editor is implemented to always soft wrap, so this contains the code for rendering a single logical line onto the screen as possibly many lines, with styling. It also allows for e.g. querying the on screen position given a character location, and can render indented, wrapped, non-interactive transcript rows after the source line.
    - line_shaper.rs: given the lexing data, static analysis data, and diagnostics associated with a single logical line, it applies styling and splits that into the text runs where within each run all characters are styled the same. This effectively creates the data necessary for a wrapped line.
    - line_map.rs: given a bunch of wrapped lines representing the content, it computes the height each line takes up as well as the y position of any logical line number. This allows for various different coordinate system conversions.
  - src/services/{mod.rs,lexing.rs,compilation.rs,execution.rs}: background services that run off the main thread
    - mod.rs: ServiceManager and ServiceManagerMessage, foreground application of background-service messages, dependency invalidation, playback/seek/parameter commands, ExecutionSnapshot re-export, version-checked text-state updates, and slide-boundary navigation helpers
    - lexing.rs: incremental lexer service that receives text-rope edits and emits lex ropes for compilation/UI state
    - compilation.rs: compilation-service root; owns message batching, import-context/cache state, parse/compile orchestration, compile diagnostics/static-analysis rope emission, and bytecode forwarding to execution
    - compilation/autocomplete.rs: categorized autocomplete suggestions (`keyword` / `variable` / `function` / `operator`) derived from parser cursor possibilities and compiler-visible identifiers
    - compilation/parameter_hint.rs: active invocation argument detection and parameter hints, including upward placement preference when the cursor is below the hinted call's first line
    - execution.rs: small module root for executor-thread service types and exports. The execution service owns the Executor (Rc-based, !Send), handles UpdateBytecode/SeekTo/TogglePlay/UpdateParameter, and sends ExecutionSnapshot back via ServiceManagerMessage::ExecutionStateUpdated, including current parameter values, optional stable-state scene data, a separate `is_loading` busy flag, and transcript sections once execution is not in a loading/compile-error snapshot. Background/camera/mesh collection itself now lives in `executor::scene_snapshot`, so UI, CLI, and exporter code can share the same validated capture path; scene data is attached when a stable scene snapshot is available, and the camera snapshot now carries an executor follower-version so viewport-local camera detachment can detect authoritative scene camera changes
    - execution/loop_state.rs: main runtime loop and playback/seek state machine; owns a persistent local playback future over an Rc/RefCell Executor plus shared Cell/RefCell control state so pause/resume, forward seeks, and parameter updates keep the live executor state instead of cancelling and rebasing. Bytecode updates, playback-mode switches, runtime-error recovery, and backward seeks drop that future and restore from cache; snapshot-injected runtime errors cancel further playback/seek/section-advance work; invalid or out-of-range targets are canonicalized to the concrete reachable scene end before snapshotting; presentation mode flips the executor into high-quality text meshing before clearing cached execution state; seek and every playback frame emit a pre-work loading snapshot so expensive LaTeX or scene execution can surface as busy UI
    - execution/snapshot.rs: parameter extraction plus ExecutionSnapshot/transcript emission using executor-provided scene snapshots and runtime-value conversion for hidden scene params such as `camera`; compile-error and loading snapshots intentionally do not publish transcript updates, and `is_loading` is carried separately from execution status so busy UI does not pretend playback is seeking
    - execution/diagnostics.rs: runtime error formatting
  - src/state: code related to global, document, text editor, and similar state
    - diagnostics.rs: errors, warnings, suggestions that are present in the text editor
    - document_state.rs: for a given document, this is just the textual state and the execution state 
    - execution_state.rs: UI-side execution state (current_timestamp, runtime_errors, slide_durations, executor-reported minimum duration hints, current parameter snapshot, slide_count, is_playing, delayed `is_loading`, transcript, plus viewport camera/background/visible-mesh snapshot, scene-image revision, and the latest scene camera version) updated via ExecutionSnapshot from the executor thread. Loading snapshots leave timestamps/durations on the last stable values, show busy UI only after a 100ms delay, and clear immediately on stable snapshots so playback frames cannot keep the loading ring stuck
    - textual_state.rs: this is not only the current text of the document, but also any part of the state of the text editor that might be queryable / editable by other entities in the system (like the current diagnostics, autocomplete, transcript inline index, and the per-item autocomplete category metadata used by the popover). Transcript inline spans are shifted across local edits similarly to diagnostics so print output stays attached to moved source lines, and inline transcript entries are indexed by the tail row of their span so multi-line `print` output renders once at the final line. Some other state is owned fully by the text editor instead, in the editor directory. The line is a bit blurry
    - user_settings.rs: persisted GPUI-global user settings for the LaTeX backend preference and explicit system `latex` / `dvisvgm` paths; applies the selected backend to the latex crate and auto-populates common system paths when requested
    - window_state.rs: global application state, consisting of the active document, open tabs, recently opened projects, and navbar scroll state; owns open/close-tab flows, single/batch project imports into recents, first-run default-scene list hydration into recents, unsaved-change prompts, discard handling, and forgetting pathless scratch tabs by deleting their internal files
  - src/timeline: a module related to timeline rendering
    - mod.rs: timeline module wiring
    - console.rs: scrollable console-style transcript panel rendering for `print` output in the bottom panel, with runtime diagnostic messages appended after transcript entries
    - timeline_view.rs: Timeline state, zoom/playhead recentering after non-loading execution snapshots, bottom-panel mode (`Timeline` / `Console`), and the top-level composition that wires the toolbar, scroll pane, painted track, transcript console, and runtime diagnostic console footer together
    - metrics.rs: timeline layout constants plus shared duration/position helpers for slide boxes, gaps, playhead placement, and zoom-scaled width calculations
    - icons.rs: transport icon asset mapping that loads vendored outline SVGs from `assets/img/timeline` through GPUI's SVG element so the toolbar stays theme-tinted while using external files
    - toolbar.rs: timeline toolbar rendering plus transport/zoom button wiring and the timeline/console panel toggle
    - track.rs: painted timeline track rendering for slides, connector ticks, duration labels, and the vertical playhead; derives per-slide styling from `slide_count` rather than assuming duration lists are populated during transient seek snapshots
  - assets/img/timeline: vendored MIT-licensed Tabler playback SVGs (`player-track-prev`, `player-skip-back`, `player-play`, `player-pause`, `player-skip-forward`, `player-track-next`) plus the bundled license notice used by the timeline toolbar
  - assets/default_scenes: bundled tutorial and example scene snippets for first-run/discovery use, including a four-part tutorial sequence (Monocurl overview, language basics, meshes/operators, animations), text/tag transforms with short `\tagN{...}` syntax, local reusable TeX fragments, an algebraic `align*` derivation, and a LaTeX first-render cache note, recursive mesh trees, presentation parameters, a procedural cut-and-move parallelogram area proof, Riemann sum formula transfers, still-image, 3d, and visual algorithm examples; scenes use consistent indented slide bodies and technique-focused comments, with the algorithm example demonstrating frontier-based BFS; startup stores the hardcoded ordered list from `window_state.rs` as the recent-project defaults when no valid saved open state exists
  - src/viewport: a module related to viewport rendering
    - scene_renderer.rs: viewport image cache for the shared renderer crate; first checks executor/camera scene revisions to skip hashing entirely when nothing changed, otherwise fingerprints mesh Arc identity/version plus both the output size and preview projection size, rasterizes through the blade renderer at the window scale factor, and hands the resulting images to GPUI
    - viewport_view.rs: thin root module for the interactive viewport; owns the `Viewport` state struct plus the public constructor/toggle API, including the shared single-image preview cache, and wires together the split submodules below
    - viewport_view/camera.rs: viewport-local camera interaction and synchronization state, including preview detachment, orbit/pan drag behavior, presentation-mode `UpdateParameter("camera", ...)` writes, reset-to-scene behavior, and scene-camera-version snap-back logic
    - viewport_view/params.rs: presentation parameter drag state, slider/plane rendering, hidden-param filtering, live bound tracking, and fixed-step overdrag updates
    - viewport_view/render.rs: top-level viewport composition and `Render` impl, including scene stage assembly, preview camera chrome, reset buttons, presentation chrome/layout, and the stage-aspect policies for both modes: preview uses the Swift/Monocurl-1.0-style full-viewport overscan treatment with a dimmed outer region and overlaid camera controls, while presentation now shrinks the visible scene to a centered 16:9 frame instead of stretching to whatever panel shape is available
    - viewport_view/style.rs: viewport-local colors, sizing constants, ring animation helpers, delayed-loading ring styling, and other styling/configuration constants shared across the split viewport modules
  - src/document_view: document-view module split by responsibility
    - mod.rs: module root; keybinding registration including Command-K timeline/console toggling, shared `DocumentView` / `OpenDocument` structs, export-overlay state, and small shared helpers
    - actions.rs: document action handlers for save/close/presentation/playback/timeline navigation, timeline/console toggling, plus dependency invalidation hooks
    - export.rs: isolated export-job orchestration from the document side, including live-text gathering, output-path prompts, worker-thread polling, and export overlay state transitions
    - render.rs: `DocumentView::new`, document/presentation layout composition, export overlay rendering, shared LaTeX warning placement above the editor navbar, and the `Render` impl
  - src/home_view.rs: code related to the actual home element (list of projects, creating/importing one or more selected documents, and deleting); now themed from `theme.rs` and shows the shared LaTeX backend warning banner above the navbar
  - src/settings_window.rs: GPUI settings window opened from the File menu or `Command+,`; currently exposes the power-user LaTeX backend toggle plus explicit binary pickers for system `latex` and `dvisvgm`
  - src/main.rs: init code that sets up menus, settings, command bindings, module init, and launches the app; wires the File-menu settings action and `Command+,` shortcut
  - src/navbar_view.rs: navbar element for switching tabs, navigating home, and toggling dark mode
  - src/theme.rs: semantic UI theme definitions, including the GPUI-global persisted `ThemeSettings`, light/dark `ThemeMode`, a white-by-default light palette for documents/viewport/editor, the dark palette, and text editor styling
  - src/window.rs: root GPUI element that owns the global state and muxes between the home and editor views
- integration_tests: cross-crate integration tests covering the full lex→parse→compile→execute pipeline
  - src/lib.rs: shared test-support utilities for integration tests, including comment/whitespace-filtered lexing, section parsing, stdlib/imported bundle construction, compile invocation, compact runtime value summaries, and printable inspection blocks for richer assertion/debug output
  - tests/basic_executor_tests.rs: root harness for basic executor coverage; owns `run(src)` and result assertions while reusing the crate-level parse/compile/stdlib helpers, then delegates the actual tests into `tests/basic_executor_tests/*.rs`
  - tests/basic_executor_tests/: split basic executor coverage by functionality (`arithmetic`, `collections`, `control_flow`, `heap`, `lambdas`, `live_values`, `operators`, `references`, `transcript`, `validation`) so API churn and runtime regressions are easier to localize
  - tests/anim_tests.rs: animation test harness root; owns `AnimResult` (timestamp, leaders, transcript, errors, error spans), `LeaderInfo` (kind, target, current), chainable result/leader assertions with inspection dumps, mesh-tree inspection helpers, and runners such as `run_anim`, `run_anim_at`, `run_anim_with_stdlib`, and `run_multi_anim`, then delegates grouped animation tests into `tests/anim_tests/*.rs`
  - tests/anim_tests/: split animation coverage by area (`timing`, `sync`, `state_vars`, `multislide`, `errors`, `regressions`) covering Wait/Set/Lerp behavior, custom lerp lambdas, mesh `Trans`/`Bend` primitives, seek mid-animation, state/param leaders, multi-slide, lock/error cases, and runtime-error span attribution across imported/root call chains
  - tests/stateful_tests.rs: stateful-value integration tests; reuses the `anim_tests.rs` harness via `#[path = "anim_tests.rs"]` so stateful cases can share the same animation/runtime snapshot helpers
- ui_cli_shared: a collection of structs and utilities that are necessary for the user facing interface, but not really execution
  - src/doc_type.rs: scene/library document type metadata, including file extensions and starter file contents with the default stdlib imports
