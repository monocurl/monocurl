{
  "name": "monocurl",
  "version": "0.2.10",
  "description": "Browser-side TypeScript controller for the Monocurl WebAssembly runtime.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**",
    "README.md"
  ],
  "scripts": {
    "build": "npm run check && tsc -p tsconfig.json && npm run bundle && npm run build:wasm",
    "build:wasm": "node scripts/build-wasm.mjs",
    "bundle": "esbuild src/index.ts --bundle --format=esm --platform=browser --target=es2022 --external:mathjax-full/* --outfile=dist/index.js",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "keywords": [
    "monocurl",
    "wasm",
    "animation",
    "math"
  ],
  "author": {
    "name": "enigmurl",
    "email": "43832426+enigmurl@users.noreply.github.com",
    "url": "https://monocurl.com"
  },
  "license": "MIT",
  "homepage": "https://monocurl.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/monocurl/monocurl.git"
  },
  "bugs": {
    "url": "https://github.com/monocurl/monocurl/issues"
  },
  "dependencies": {
    "mathjax-full": "^3.2.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^5.7.0"
  }
}
